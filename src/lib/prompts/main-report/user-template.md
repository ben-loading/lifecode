根据以下命盘数据生成主报告。请严格按照 system 中的输出格式规定：**palaceAnalysis** 必须为对象（5 个子模块，每模块含 title 与 description，title 填宫位/星象短标签）；**careerDestiny** 必须为对象（tracks、industries、position 三字段，内容不可重复）。禁止将 palaceAnalysis 或 careerDestiny 写成一段字符串。

**当前公历时间**：{{CURRENT_DATE}}（年：{{CURRENT_YEAR}}，月：{{CURRENT_MONTH}}）

**重要**：流年三年（yearlyFortuneChart 和 yearlyDetails）必须为：**去年（{{LAST_YEAR}}）**、**今年（{{CURRENT_YEAR}}）**、**明年（{{NEXT_YEAR}}）**，禁止使用今年、明年、后年。

{{IZTRO_INPUT}}
